name: Run Playwright Test

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: 14
      - name: Install dependencies
        run: npm i
      - name: Install Playwright
        run: npx playwright install --with-deps
      - name: Run Playwright Test
        run: npx playwright test


#this is testing in multiple envs
# jobs:
#   example_matrix:
#     strategy:
#       matrix:
#         os: [ubunt-latest, windows-latest]
#         version: [10,12,14]
#     runs-on: ${{matrix.os}}